using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class BallSelectionUI : MonoBehaviour
{
    //[SerializeField] Text redBallAmmoText;
    //[SerializeField] Text greenBallAmmoText;
    //[SerializeField] Text blueBallAmmoText;

    //[SerializeField] Player player;

    private void Awake()
    {
        //if (player != null)
        //{
        //    player.OnRedBallAmmoChanged.AddListener(OnRedBallAmmoChangedCallback);
        //    player.OnGreenBallAmmoChanged.AddListener(OnGreenBallAmmoChangedCallback);
        //    player.OnBlueBallAmmoChanged.AddListener(OnBlueBallAmmoChangedCallback);
        //}
    }

    // Start is called before the first frame update
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //void OnRedBallAmmoChangedCallback(int ammo)
    //{
    //    if(redBallAmmoText != null)
    //    {
    //        redBallAmmoText.text = ammo.ToString();
    //    }
    //}

    //void OnGreenBallAmmoChangedCallback(int ammo)
    //{
    //    if (greenBallAmmoText != null)
    //    {
    //        greenBallAmmoText.text = ammo.ToString();
    //    }
    //}

    //void OnBlueBallAmmoChangedCallback(int ammo)
    //{
    //    if (blueBallAmmoText != null)
    //    {
    //        blueBallAmmoText.text = ammo.ToString();
    //    }
    //}
}
